---
# Pin the runtime that CF should install during staging. Without this the
# buildpack may try an unavailable or unsupported version and finalization fails.
dotnet-core:
  sdk: 8.0.408
  runtime: 8.0.15
